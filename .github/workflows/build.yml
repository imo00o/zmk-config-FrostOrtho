name: Build ZMK Firmware

on:
  push:
    branches:
      - main-jp
      - main-us
      - main-jp-aml
      - main-us-aml
      - develop-jp
      - develop-us
      - muino-setting
    paths:
      - 'config/**'
  pull_request:
    branches:
      - main-jp
      - main-us
      - main-jp-aml
      - main-us-aml
      - develop-jp
      - develop-us
      - muino-setting
    paths:
      - 'config/**'
  workflow_dispatch:

jobs:
  build:
    uses: zmkfirmware/zmk/.github/workflows/build-user-config.yml@main
