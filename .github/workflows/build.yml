name: Build ZMK Firmware

on:
  push:
    branches:
      - main
    paths:
      - 'config/**'
  pull_request:
    branches:
      - main
    paths:
      - 'config/**' # configフォルダ内の全てのファイル変更をトリガーとする
  workflow_dispatch:

jobs:
  build:
    uses: zmkfirmware/zmk/.github/workflows/build-user-config.yml@main
